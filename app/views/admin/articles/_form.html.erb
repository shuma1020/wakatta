<%= render "shared/errors", obj: @article %>

<table class="attr">
  <tr>
    <th><%= form.label :title %></th>
    <td><%= form.text_field :title, size: 20 %></td>
  </tr>
  <% if request.path.include?("edit")%>
    <tr>
      <th><%= form.label :動画リンク %></th>
      <td><%= form.url_field :youtube_url_edit, size: 20 %></td>
    </tr>
    <%else%>
    <tr>
      <th><%= form.label :動画リンク %></th>
      <td><%= form.url_field :youtube_url, size: 20 %></td>
    </tr>
  <%end%>
  <tr>
    <th><%= form.label :body %></th>
    <td><%= form.text_area :body, rows: 10, cols: 45 %></td>
  </tr>
  <tr>
    <th><%= form.label :released_at, for: "article_released_at_1i" %></th>
    <td><%= form.datetime_select :released_at,
              start_year: 2000, end_year: Time.current.year + 1,
              use_month_numbers: true %></td>
  </tr>
  <tr>
    <th><%= form.label :expired_at, for: "article_expired_at_1i" %></th>
    <td>
      <div>
        <%= form.check_box :no_expiration %>
        <%= form.label :no_expiration %>
      </div>
      <div>
      <td>
        <%= form.datetime_select :expired_at,
          start_year: 2000, end_year: Time.current.year + 1,
          use_month_numbers: true %>
      </td>
      </div>
    </td>
  </tr>
      
  <tr>
    <th><%= Article.human_attribute_name(:member_only) %></th>
    <td>
      <%= form.check_box :member_only %>
      <%= form.label :member_only %>
    </td>
  </tr>
</table>
    <%= form.label :category, 'カテゴリ' %></th>
    <%= form.collection_check_boxes(:category_ids, Category.all, :id, :name) do |category| %> 
      <td><%= category.label do %>
        <%= category.check_box %>
        <%= category.text %>
        <%end%>
      <%end%>
